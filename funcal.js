function calculation(){
    let n=document.getElementsByid("subject").value;
    let i;
    let total;
    for(i=0;i<n;i++){
        let x=parseFloat(prompt("enter the marks of subject" + (1)));
        total+=n;
        let average = total/n;
        let grade;
        if(average>90)
            grade ='A+';
        else{

        }
        let result = document.getelementbyid.("result").inner
    }
}